import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Vokabeltrainer',
  appName: 'Vokabeltrainer',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
